<div class='row'>
  <div class='col col-md-4'>
    <%= form_with(model: veichle, local: true, id: 'formveichle') do |form| %>
    <% if veichle.errors.any? %>
    <div id='error_explanation'>
      <h2>
        <%= pluralize(veichle.errors.count, 'error') %> non posso salvare il record corrente:</h2>
      <ul>
        <% veichle.errors.full_messages.each do |message| %>
        <li>
          <%= message %>
        </li>
        <% end %>
      </ul>
    </div>
    <% end %>
    <div class='field'>
      <%= form.label 'MEZZO:' %>
      <%= form.text_field :name, class: 'form-control', id: 'name' %>
    </div>
    <div class='field'>
      <%= form.label 'PRODUTTORE:' %>
      <%= form.text_field :producer, class: 'form-control' %>
    </div>
    <div class='field'>
      <%= form.label 'TARGA:' %>
      <%= form.text_field :licenceplate, class: 'form-control' %>
    </div>
    <div class='field'>
      <%= form.label 'POSIZIONE:' %>
      <%= form.number_field :position, class: 'form-control' %>
    </div>
    <br>
    <div class='form-group'>
      <%= button_tag(type: 'submit', class: 'btn btn-primary btn-sm') do %>
      <%= fa_icon('save') %>&nbsp;Salva
      <% end %>
    </div>
    <% end %>
  </div>
</div>
<script>
  $('#formveichle').submit(function () {
    if ($('#name').val() == "-") {
      alert("Questo Carattere è Riservato");
      return false;
    };
    return;
  });
</script>