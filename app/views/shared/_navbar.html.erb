<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to inline_svg('navbar_logo.svg', size: '22px * 22px', preserve_aspect_ratio: 'xMaxYMax meet'), root_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li>
          <a href='#'>
            <small>
              <strong>
                <%= fa_icon('calendar') %>&nbsp;&nbsp;
                <%= l(Date.today, format: '%A %d %B %Y') %>
              </strong>
            </small>
          </a>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class='dropdown'>
          <a href='#' class='dropdown-toggle' data-toggle='dropdown' role='button' aria-expanded='false'>
            <strong>
              <%= fa_icon('user') %>&nbsp;&nbsp;
              <%= current_user.name %>&nbsp;&nbsp;|&nbsp;&nbsp;
              <small>
                <%= current_user.institute %>
              </small>
              <span class='caret'></span></a>
          </strong>
          <ul class='dropdown-menu' role='menu'>
            <% if current_user %>
              <% if current_user.admin == true %>
                <li>
                <%= link_to  fa_icon('cog') + ' ' + ' AMMINISTRAZIONE', rails_admin_path %>
                </li>
              <% end %>
            <% end %>
            <li>
              <%= link_to fa_icon('info-circle') + ' ' + ' INFO', home_about_path %>
            </li>
            <li>
              <%= link_to fa_icon('user') + ' ' + ' ESCI', destroy_user_session_path, method: :delete %>
            </li>
          </ul>
        </li>
        <li>&nbsp;</li>
      </ul>
    </div>
  </div>
</nav>
